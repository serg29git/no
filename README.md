languages:
ru (русский, ру) : 2
en (English, ен) :1

------------------------------------------------------------------------------------------------------------
EN

1. No — a tiny Unix command that prints "n" indefinitely.

Short description A minimalist command-line utility named "no" that outputs a continuous stream of the letter "n" (one per line). Useful for quick stress tests, terminal demos, or scripting experiments.

Long description No is a single-file executable that repeatedly prints "n" in an infinite loop. It’s kinda silly, inspired by the yes command. — perfect for automating option choosing in scripts, and goofin around.

Features

Single-file Bash script with no external dependencies
Prints "n" followed by new line continuously until interrupted
Includes simple installer script to place/remove the command (uses sudo when needed)

Quick install

Save the repository and run the installer
Run the command: no
Stop with Ctrl-C.

Contributing Bug reports, installation improvements, or alternative implementations (C, Go, Rust) welcome — keep it small and focused

this project exists to exist.

use with caution (it produces infinite output).

----------------------------------------------------------------------------------------------------------
RU

2. No - мини unix - команда, которая бесконечно выдает одну 'n' на линию текста.

описание (маленькое)
No — это однофайлный исполняемый файл, который многократно печатает «n» в бесконечном цикле. Это немного глупо, вдохновлённое командой «yes». — идеально подходит для автоматического выбора опций в скриптах и для шуток. Особенности : Однофайлный скрипт Bash без внешних зависимостей. Печатает бесконечно одну «n» на линию текста непрерывно до прерывания. Включает простой скрипт установки для размещения/удаления команды (использует sudo при необходимости) Быстрая установка : Сохраните репозиторий и запустите установщик, и Выполните команду no. Остановить скрипт можно с Ctrl-C. Вклад в отчёты об ошибках, улучшения установок или альтернативные реализации (C, Go, Rust) приветствуются — держите материал компактным и сфокусированным.

описание (большое)
No — это исполняемый файл, который многократно выводит "n" в бесконечном цикле. Он довольно забавный, вдохновлён командой yes — идеально подходит для автоматизации выбора параметров в скриптах и ​​для развлечений.

Особенности

Скрипт Bash в одном файле без внешних зависимостей
Непрерывно выводит одну "n" на линию текста, пока не будет прерван.
Включает простой скрипт установки для добавления/удаления команды (использует sudo при необходимости)

Быстрая установка

Сохраните репозиторий и запустите установщик
Выполните команду: no
Остановите с помощью Ctrl-C.

Вклад.

Сообщения об ошибках, улучшения установки или альтернативные реализации (C, Go, Rust) приветствуются — пусть проект будет небольшим и целенаправленным

Этот проект существует для существования.

Используйте с осторожностью (он даёт бесконечный выход). 



